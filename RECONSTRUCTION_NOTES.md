# Reconstruction Notes

This source tree was reconstructed from compiled assets in `dist/`.

What is recovered:
- React + React Router app structure
- Route map (`/`, `/filters`, `/gallery`, `/project/:id`, `/about`)
- Sheety API integration and loader flow
- Project detail behavior with desktop Swiper and mobile stacked media
- Existing built CSS reused via import from `dist/assets/index-CTp1jiep.css`

What is approximate:
- Original component names and exact internal logic
- Original build config and Tailwind source setup
- Exact data field mapping for every media variant
- About page content and some UI micro-interactions

